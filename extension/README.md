# Cursor Traffic Light Extension (Legacy)

> **注意**: VS Code 扩展方案已被 **Cursor Hooks 桥接** 替代。
> 新方案通过 `hooks-bridge/agent-light.js` 直接利用 Cursor 原生 Hooks 获取 Agent 事件,
> 无需安装扩展, 准确率更高。详见项目 README。

此扩展保留作为参考实现和 VS Code 兼容方案。
